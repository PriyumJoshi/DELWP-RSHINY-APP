P = only use prescene, BG = our background data assumption , PA = psuedo points that are very close to obs points using function in spatialEco

5-k fold
Each algorithm is repeated once
Number of trees  = 100